# name: Deploy

# on:
#   push:
#     branches:
#       - main
# defaults:
#   run:
#     working-directory: web
# jobs:
#   build:
#     name: Build
#     runs-on: ubuntu-latest    
#     permissions:     
#       contents: write
#     steps:
#         - name: Checkout
#           uses: actions/checkout@v4
  
#         - uses: pnpm/action-setup@v4
#           name: Install pnpm
#           with:
#             version: 9
#             run_install: false
  
  
#         - name: Install dependencies
#           run: pnpm install
#           working-directory: ./web
#         - name: Build project
#           run: npm run build
#           working-directory: ./web
#         - name: Upload artifact
#           uses: actions/upload-pages-artifact@v3
#           with:
#             path: ./web/dist # The path to the built files.
    
#   # Deployment job
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
